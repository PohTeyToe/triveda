/**
 * Prompt templates -- version router.
 *
 * Currently only v1 exists. When v2 prompt templates are created,
 * they will be added here and callers can select by version string.
 */

export * from './v1/index.js';
