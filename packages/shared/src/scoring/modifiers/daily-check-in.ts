/**
 * Daily check-in modifier stub.
 *
 * Placeholder returning 1.0 (no effect). The real implementation
 * will be provided in split 11.
 *
 * Pure function, no IO.
 */

/** Stub: always returns 1.0 (identity multiplier). */
export function dailyCheckInModifierStub(): number {
  return 1.0;
}
