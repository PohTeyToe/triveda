/**
 * Blood work modifier stub.
 *
 * Placeholder returning 1.0 (no effect). The real implementation
 * will be provided in split 10.
 *
 * Pure function, no IO.
 */

/** Stub: always returns 1.0 (identity multiplier). */
export function bloodWorkModifierStub(): number {
  return 1.0;
}
